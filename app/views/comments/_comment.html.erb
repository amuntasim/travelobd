<div class="comment">
  <div class="eventDetail eventTitle clear">
    <%= comment.comment_by %>
    <%= t('general.label.says') %>
    <small style="color:#101050; font-weight: normal; float:right;">
      <%= t('general.label.time') %> : <%= l(comment.created_at).localize %></small>
  </div>
  <div class="eventDetail clear">
    <%= comment.content %>
  </div>
</div>