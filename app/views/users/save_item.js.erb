<% if @saved_item%>
$('#saved_item_li').html('<%= link_to '[' +  t('general.label.remove') + ']', 'javascript:void(0)', :class => 'remove_from_save_listing',:sl_id=> @saved_item.id,:savable_id => @saved_item.savable_id, :savable_type => @saved_item.savable_type %>  <%= link_to t('general.label.already_saved'), my_saved_listings_path %>  &nbsp;');
<% end %>