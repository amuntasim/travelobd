<%= form_for(@ad_video) do |f| %>
  <% if @ad_video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad_video.errors.count, "error") %> prohibited this ad_video from being saved:</h2>

      <ul>
      <% @ad_video.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ad_id %><br />
    <%= f.text_field :ad_id %>
  </div>
  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
