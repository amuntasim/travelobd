<%= content_for :over_mid_content do %>
  <div class="redhead_row">
    <h2>Add <%= @ad.featured? ? 'Featured' : 'Basic'  %> Package</h2>
  </div>
<% end %>
<div class="spot_box" id="article_form">
<%= render 'form' %>
</div>